import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  images: {
    domains: ['coin-images.coingecko.com'], // Substitua 'api.exemplo.com' pelo domínio da API que fornece as imagens
  },
  /* config options here */
};

export default nextConfig;
